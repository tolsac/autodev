apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: autodev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: autodev/backend:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: backend-secrets
            - configMapRef:
                name: backend-config
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: autodev
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: autodev

resources:
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  # backend runs locally (not in k3d) — see Tiltfile
  # - backend-config.yaml
  # - backend.yaml
